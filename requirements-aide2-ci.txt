setuptools<81
fastapi==0.99.1
uvicorn==0.27.0
pydantic==1.10.14
pytest==8.0.0
pytest-cov==4.1.0
httpx==0.27.2
numpy==1.26.3
pandas==1.5.3
pyarrow==11.0.0
pyspark==3.5.0
delta-spark==3.0.0
pyyaml>=6.0
requests==2.31.0
prometheus-client==0.19.0
kafka-python==2.0.2
confluent-kafka==2.3.0
feast==0.34.0
# dask==2023.12.1 was previously pinned "for Feast compatibility" but that
# version has a real bug importing under Python 3.11 (TypeError inside
# dask.dataframe.accessor's pandas-accessor binding), which silently broke
# `import feast` entirely -- masked in aide2/feast/features.py by a broad
# `except Exception:` fallback to dummy shim classes. 2024.5.1 fixes it.
dask==2024.5.1

